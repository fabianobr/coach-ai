# coach-ai — project rules for Continue

All architecture, conventions, patterns, MUST rules, and contribution guidance
for this project are defined in `AGENTS.md` at the repository root.

**Read `AGENTS.md` before exploring or modifying any code.** It contains:
- Project overview and repository layout
- The three runtimes and CLI (how to run each)
- LLM abstraction layer design
- How to add a new LLM provider
- Coding, testing, and commit conventions
- Rules that apply to every AI coding assistant working in this repo
